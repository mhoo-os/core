# Welcome

Mhoo keeps document transformation local, deterministic, and scoped to a trusted
tenant binding. This short prose fixture has one heading and two paragraphs.

The evaluation must not call a provider, create an embedding, or persist a
document. It exists only to compare chunk construction behavior.
