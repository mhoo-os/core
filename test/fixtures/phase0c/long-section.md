# Long Section

This paragraph is deliberately repeated to force deterministic chunk boundaries while preserving a stable heading context. The native baseline should group paragraphs before splitting an oversized paragraph, while the LlamaIndex comparison may prefer sentence boundaries. Each output remains application-owned and does not inherit an upstream node identifier.

This paragraph is deliberately repeated to force deterministic chunk boundaries while preserving a stable heading context. The native baseline should group paragraphs before splitting an oversized paragraph, while the LlamaIndex comparison may prefer sentence boundaries. Each output remains application-owned and does not inherit an upstream node identifier.

This paragraph is deliberately repeated to force deterministic chunk boundaries while preserving a stable heading context. The native baseline should group paragraphs before splitting an oversized paragraph, while the LlamaIndex comparison may prefer sentence boundaries. Each output remains application-owned and does not inherit an upstream node identifier.

This paragraph is deliberately repeated to force deterministic chunk boundaries while preserving a stable heading context. The native baseline should group paragraphs before splitting an oversized paragraph, while the LlamaIndex comparison may prefer sentence boundaries. Each output remains application-owned and does not inherit an upstream node identifier.

This paragraph is deliberately repeated to force deterministic chunk boundaries while preserving a stable heading context. The native baseline should group paragraphs before splitting an oversized paragraph, while the LlamaIndex comparison may prefer sentence boundaries. Each output remains application-owned and does not inherit an upstream node identifier.
